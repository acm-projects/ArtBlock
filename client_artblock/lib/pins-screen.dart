import 'package:flutter/material.dart';

class Pins extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('pins tab')),
    );
  }
}
